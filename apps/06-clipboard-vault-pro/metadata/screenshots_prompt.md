# Screenshots Prompt — Clipboard Vault Pro

Create 6 Microsoft Store screenshots (1920×1200 or 16:10 ratio).
Each must show real UI value in a clean Windows desktop layout.

## Screenshot 1 — Dashboard
Show the main dashboard with:
- Violet welcome banner ("Good morning — welcome back" with day count)
- Stats row (Snippets Saved, Copied, Collections, Snippets Now)
- 7-day SVG activity bar chart
- Quick Actions tiles (Add to Vault highlighted) + type-breakdown grid
- Privacy-first info card on the right
- Recent snippets list with three sample rows

## Screenshot 2 — Vault (list view)
Show the Vault page with:
- Add snippet form at the top: title field + multi-line content field, auto-detected type badge
- 6–8 snippet rows mixing URL, EMAIL, CODE, PHONE, TEXT badges
- One row expanded showing full content, tags (e.g. "ssh", "staging", "internal"), action buttons

## Screenshot 3 — Vault (grid view)
Same data, grid layout:
- 6 compact snippet cards
- Each with type badge, title, mono preview, Copy/Pin/Favourite/Delete actions

## Screenshot 4 — Collections detail
Show a Collections page with a collection opened:
- Top bar with collection name ("SSH Keys") + Rename / Add / Delete actions
- "In this collection" section with three snippet rows
- "Add from vault" section showing pickable items beneath

## Screenshot 5 — Search & Filter
Show the Search page with:
- Search field with "ssh" typed in
- Type chips with CODE and TEXT active
- Pinned-only segment selected, Favourite Off
- Date range picked
- Results list with 5 matches showing copy + delete actions

## Screenshot 6 — Settings & Onboarding card
Settings page on the left (theme, max items dropdown, auto-detect toggle, privacy mode, clear data),
and a small inset showing the second Onboarding slide ("Organise with tags & collections" + folder SVG)
to convey the polished first-run flow.

Requirements:
- No fake ratings, reviews, or award badges
- No "#1" or "best" claims in screenshots
- Clean readable UI text
- Premium productivity-tool aesthetic
