# Screenshots Prompt — PDF Master Kit

Create 6 Microsoft Store screenshots (1920×1200 or 16:10 ratio).
Each must show real UI value in a clean Windows desktop layout.

## Screenshot 1 — Dashboard
Show the main dashboard with:
- Stats cards (Operations Run, Pages Processed, Files Processed)
- Quick action tiles (Merge, Split, Compress, Rotate, Reorder, Extract)
- Privacy-first info card
- Recent operations list with a few sample entries
- Blue/indigo accent, dark theme

## Screenshot 2 — Merge
Show the Merge page with:
- File drop zone at the top
- A list of 3–4 PDFs queued in order with arrows to reorder
- Total page count and total size summary
- Merge button highlighted in the top bar

## Screenshot 3 — Split
Show the Split page with:
- Loaded PDF file info card
- Split mode selector with "By ranges" highlighted
- Input field with sample ranges (e.g. "1-3, 4-6, 7-10")
- Output explanation card

## Screenshot 4 — Reorder
Show the Reorder page with:
- Loaded PDF file info card
- Page list with at least 8 pages
- One page being dragged with a drop target visible
- Per-page rotate icon visible on each row

## Screenshot 5 — Extract / Rotate
Show either Extract or Rotate (whichever looks best):
- Page grid with several pages selected/rotated
- Selection counter and Select all / None controls
- Operation button in top bar

## Screenshot 6 — History & Settings
Show the History page with an expanded card revealing operation details
(input, output, page counts, size delta, notes) AND a small inset showing
the Settings page theme switcher.

Requirements:
- No fake ratings, reviews, or award badges
- No "#1" or "best" claims in screenshots
- Clean readable UI text
- Professional Windows utility aesthetic
