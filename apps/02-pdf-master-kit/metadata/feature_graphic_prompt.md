# Feature Graphic Prompt — PDF Master Kit

Create a professional Microsoft Store promotional banner image (3480×2160).

Scene:
- Windows desktop showing PDF Master Kit running
- Left side: a stack of separate PDF documents flowing into a single merged PDF
- Center: the Reorder page with pages being dragged into a new sequence
- Right side: a single PDF being split into three smaller PDF files

Visual style:
- Dark theme (#0f1117 background)
- Blue/indigo accent (#3b82f6, #6366f1)
- Clean, modern typography (Inter font)
- Professional document utility mood
- Subtle motion-line elements suggesting reordering and splitting (static representation)

Text overlay (bottom area):
- "PDF Master Kit"
- "Merge, split, rotate, reorder, extract — locally"
- No "#1", "best", "top", or fake award claims
- No user rating numbers or fake review counts
